# Data and model outputs (gitignored; regenerated by pipeline)
data/
!data/.gitkeep
models/
!models/.gitkeep
outputs/
!outputs/.gitkeep
logs/
!logs/.gitkeep

# R session artifacts
.RData
.RDataTmp
.Rhistory
.Rapp.history
.Ruserdata
*-Ex.R

# R CMD build/check
/*.tar.gz
/*.Rcheck/

# RStudio
.Rproj.user/

# renv internals (library is local)
renv/library/
renv/staging/
renv/local/

# Quarto / knitr caches
_freeze/
*_cache/
/.quarto/

# LaTeX intermediates (keep title.tex)
*.tex
!reports-src/title.tex

# OS / editor
.DS_Store
._.DS_Store

# Environment overrides
.envrc
.Renviron

# Claude Code instructions (local only)
CLAUDE.md
